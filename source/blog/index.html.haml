---
blog: 'Eick Family'
---

%h1.title= current_page.data.blog

- if page_articles.any?
  - page_articles.group_by {|a| a.date.year }.each do |year, articles|
    %section
      .section-title= year
      %ul.blog-list
        - articles.each_with_index do |article, i|
          %li= partial "shared/post_index", locals: { article: article }
- else
  %section
    .section-title Articles
    %p Sorry, no articles yet
