%ul.left-nav
  %li.nav-home
    = link_to "/", class: "ss-home" do
      -# So we can hide the lable in the header without hiding the icon
      %span Home
%ul
  %li
    = link_to "/family", class: "ss-openbook#{' active' if current_page.url.start_with?('/family')}" do
      %span Family
  %li
    = link_to "/snapshot", class: "ss-write#{' active' if current_page.url.start_with?('/snapshot')}" do
      %span Snapshot
  %li
    = link_to "/history", class: "ss-globe#{' active' if current_page.url.start_with?('/history')}" do
      %span History
  -#
    %li
      = link_to "/portfolio", class: "ss-camera#{' active' if current_page.url.start_with?('/portfolio')}" do
        %span Portfolio
  %li
    = link_to "/contact", class: "ss-send#{' active' if current_page.url.start_with?('/contact')}" do
      %span Contact
  %li
    = link_to "/about", class: "ss-user#{' active' if current_page.url.start_with?('/about')}" do
      %span About
